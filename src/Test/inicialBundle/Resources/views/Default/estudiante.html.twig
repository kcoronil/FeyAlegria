{% extends '::plantilla.html.twig' %}
{% block content %}
    <div class="col-sm-4 col-sm-offset-4">
    {% if not app.session.get('autenticado') %}
        <div class="panel panel-info">
            <div class="panel-heading">
                <h2 class="panel-title">Ingreso al Sistema</h2>
            </div>
            <div class="panel-body">
                <form class="form-signin" method="POST" action="{{path('inicial_homepage')}}">
                    <div class="form-group">
                        <label for="inputEmail" class="sr-only">Nombre de Usuario</label>
                        <input type="text" id="usuario" class="form-control" name="usuario" placeholder="Nombre de Usuario" required autofocus>
                    </div>
                    <div class="form-group">
                        <label for="inputPassword" class="sr-only">Clave</label>
                        <input type="password" id="Password" class="form-control" name="password" placeholder="Clave" required>
                    </div>
                    <div class="text-center">
                        <button class="btn btn-primary btn-default" type="submit">Iniciar Sesion</button>
                        <a class="btn btn-default" href="{{ path('inicial_solicitar_pass') }}">Recuperar Contraseña</a>
                    </div>
                </form>
            </div>
        </div>
    {% endif %}
      </div>
    </div>
      
    {% if app.session.get('autenticado') %}
      <h1 align="center">Estudiante </br><span class="glyphicon glyphicon-education gi-3x" aria-hidden="true"></span></h1>        
        <div class="page-header"></div>
              <div class="row">
                <div class="col-xs-6 col-sm-4" align="right">
                  <a href="{{ path('inicial_agregar_alumno') }}"><button type="button" class="btn btn-lg btn-primary"><span class="glyphicon glyphicon-plus-sign" aria-hidden="true"></span><br />Agregar Estudiante</button></a>
                </div>
                <div class="col-xs-6 col-sm-4" align="center">
                  <a href="{{ path('inicial_agregar_alumno_usuario') }}"><button type="button" class="btn btn-lg btn-primary"><span class="glyphicon glyphicon-education" aria-hidden="true"></span><br />Agregar Estudiante Representante</button></a>
                </div>
                <div class="col-xs-6 col-sm-4" >
                  <a href="{{ path('inicial_lista_alumno') }}"><button type="button" class="btn btn-lg btn-primary"><span class="glyphicon glyphicon-search" aria-hidden="true"></span><br />Consulta</button></a>
                </div>
              </div>
              <br>
              <div class="row" align="center">
                  <a href="{{ path('inicial_pdf_alumno') }}" target="_blank"><button type="button" class="btn btn-lg btn-warning"><span class="glyphicon glyphicon-list-alt" aria-hidden="true"></span><br />Listado en PDF</button></a>
              </div>
                <a href="{{ path('inicial_homepage') }}"><h1><span class="glyphicon glyphicon-arrow-left gi-3x" aria-hidden="true"></a>
              
             
    {% endif %}
{% endblock %}